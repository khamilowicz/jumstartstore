require 'spec_helper'

describe Order do
  context "to be valid" do
    
    it "An order must belong to a user"
    it "An order must be for one or more of one or more products currently being sold"
  end

  
end
